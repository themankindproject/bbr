# cargo-audit configuration
# See: https://github.com/rustsec/rustsec/blob/main/cargo-audit/audit.toml.example

[advisories]
ignore = [
    # time 0.3.x DoS via stack exhaustion in parsing.
    # Fix requires time >=0.3.47 which needs Rust 1.88+ (our MSRV is 1.75).
    # Acceptable risk: bbr only parses trusted API responses, not arbitrary user input.
    "RUSTSEC-2026-0009",
]
